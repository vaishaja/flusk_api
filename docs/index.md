# My Project : CodeReview

## Overview
This project is an simple demostrative application that does the code review of the existing git repo and the new commits

## Installation
To install the project, run:
```bash
pip install -r requirements.txt